# GFMS Environment Variables
# Copy to .env and adjust as needed

# Backend API URL (leave empty to use Vite proxy in development)
VITE_API_URL=

# Set to 'true' to use mock data stream instead of live server
VITE_USE_MOCK=false
